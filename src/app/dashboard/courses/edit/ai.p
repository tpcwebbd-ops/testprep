Now your task is 
1. update course/page.tsx so it can render data from coursesSlice and if I click Enrollments then it will go to purchase page and auto select that course throw url by courseId.
2. it catch the course by courseId and auto select it. if no data found in url then select first course. 
3. make a new box called cash for payment. it just create a pending payment and pending student status is running. 