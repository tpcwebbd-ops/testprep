I will give you some code named Add.tsx, and some of snoppet short of code. You just update Add.tsx as following instruction.

Add.tsx page will work as this instruction. 

1. At the top there is a search bar. 
2. If I write min 3 word and delay 1 second then it will fetch user (The code I will provide below).
3. Then It show the user list. 
4. There is a button at the right side of each list named Add 
5. If I click the Add button. then it will post a quest with data,

Add.tsx 
```

```

