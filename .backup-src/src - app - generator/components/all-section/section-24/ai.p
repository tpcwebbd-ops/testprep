Now Please generate data.ts, Mutation.tsx, and Query.tsx for Imaes. 
And also implement those features.
1. Uploades single images by thins components.   <ImageUploadManagerSingle value={formData.documents[docKey] || ''} onChange={url => updateDocument(docKey, url)} /> (this component can upload one images at a time)
2. I can select upload multiple images one by one. 
3. resize the images. 
4. images positioning.

